apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-immich-model
  namespace: immich
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 20Gi